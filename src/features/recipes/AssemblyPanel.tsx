/**
 * AssemblyPanel is a thin alias for the AssemblyBoard. In the document-first
 * model the board is hosted by the right panel's Recipes tab (or any
 * downstream consumer that wants a full-screen recipe editor). The prop
 * surface is identical.
 */
export { AssemblyBoard as AssemblyPanel } from "./AssemblyBoard";
